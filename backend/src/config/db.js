// Configuración de conexión a PostgreSQL (a implementar con pg o Sequelize)
// const { Pool } = require('pg');
// const pool = new Pool({ ... });

module.exports = {
  // TODO: inicializar y exportar la conexión a la base de datos
};
