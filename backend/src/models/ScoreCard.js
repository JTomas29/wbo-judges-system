// Modelo de Tarjeta de Puntuación — puntuación general de un juez por pelea
class ScoreCard {
  // TODO: definir campos: id, fightId, judgeId, totalScore, winner, submittedAt
}

module.exports = ScoreCard;
