// Modelo de Puntuación por Asalto — detalle de cada asalto puntuado
class RoundScore {
  // TODO: definir campos: id, scoreCardId, roundNumber, scoreBoxerA, scoreBoxerB
}

module.exports = RoundScore;
